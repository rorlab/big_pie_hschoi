

<ul class="items"> 
  <% @items.each do |item| %>
  <li>
    <%= link_to image_tag(item.photo), item %>
    <p> <%= item.user.email %> </p>  
    <p> <%= item.description %> </p>
  </li>
  <% end %>
</ul>